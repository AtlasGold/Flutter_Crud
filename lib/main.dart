import 'package:flutter/material.dart';
import 'login.dart';

void main() {
  runApp(MaterialApp(
    debugShowCheckedModeBanner: false,
    title: 'Crud in Flutter',
    home: Scaffold(
      body: FirstRoute(),
    ),
  ));
}
